function createCustomMenu(){
	let options = document.getElementById("options");
	let defaults = document.createElement("div");
	defaults.appendChild(document.createTextNode("Restore Defaults"));

	document.getElementById("options");
}